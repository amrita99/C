#include <stdio.h>

int main()
{   float area,length,breadth;
    printf("Enter the length ");
    scanf("%f/n",&length);
    printf("Enter the breadth ");
    scanf("%f/n",&breadth);
    area=length*breadth;
    
    printf("area = %f cm",area);

    return 0;
}
